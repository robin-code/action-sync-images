name: Trigger Drone Build via Cloudflare

# workflow_dispatch 表示手动触发
on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to build'
        required: true
        default: 'main'

jobs:
  trigger-drone-build:
    runs-on: ubuntu-latest
    env:
      DRONE_SERVER_URL: ${{ vars.DRONE_SERVER_URL }}
      DRONE_REPO: ${{ vars.DRONE_REPO }}
      DRONE_BRANCH: ${{ github.event.inputs.branch }}

    steps:
      - name: Call Drone API behind Cloudflare
        env:
          DRONE_API_TOKEN: ${{ secrets.DRONE_API_TOKEN }}
          CF_ACCESS_CLIENT_ID: ${{ secrets.CF_ACCESS_CLIENT_ID }}
          CF_ACCESS_CLIENT_SECRET: ${{ secrets.CF_ACCESS_CLIENT_SECRET }}
        run: |
          set -euo pipefail
          : "${DRONE_API_TOKEN:?DRONE_API_TOKEN secret is required}"

          DRONE_SERVER_URL="${DRONE_SERVER_URL:-https://drone-api.riji.life}"
          DRONE_REPO="${DRONE_REPO:-DevOps/action-sync-images}"

          curl_args=(
            --fail-with-body
            --silent
            --show-error
            --location
            --request POST
            "${DRONE_SERVER_URL}/api/repos/${DRONE_REPO}/builds?branch=${DRONE_BRANCH}"
            --header "Authorization: Bearer ${DRONE_API_TOKEN}"
            --header 'User-Agent: GitHubAction/1.0'
            --header 'Accept: application/json'
            --connect-timeout 15
            --max-time 60
            --retry 3
            --retry-delay 5
            --retry-all-errors
          )

          if [[ -n "${CF_ACCESS_CLIENT_ID:-}" && -n "${CF_ACCESS_CLIENT_SECRET:-}" ]]; then
            curl_args+=(
              --header "CF-Access-Client-Id: ${CF_ACCESS_CLIENT_ID}"
              --header "CF-Access-Client-Secret: ${CF_ACCESS_CLIENT_SECRET}"
            )
          fi

          curl "${curl_args[@]}"
